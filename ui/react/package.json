{
	"name": "@poc/ui/react",
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"dev": "pnpm run generate && vite --port 5176 --host",
		"generate": "tsx ./generate.ts",
		"build": "pnpm run generate && vite build",
		"preview": "pnpm run build && vite preview --port 5176 --host"
	},
	"devDependencies": {
		"@module-federation/vite": "catalog:vite",
		"@types/node": "^22.8.7",
		"custom-element-react-wrappers": "^1.6.8",
		"@preact/preset-vite": "catalog:vite",
		"tsx": "^4.19.1",
		"vite": "catalog:vite",
		"zod": "^3.23.8"
	},
	"dependencies": {
		"preact": "^10.24.3"
	}
}
